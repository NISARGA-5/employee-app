steps:

- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: bash
  args:
    - '-c'
    - |
      gcloud compute ssh g1mp22cg037@bastion-vm \
      --zone=asia-south1-a \
      --tunnel-through-iap \
      --command="hostname"

options:
  logging: CLOUD_LOGGING_ONLY
